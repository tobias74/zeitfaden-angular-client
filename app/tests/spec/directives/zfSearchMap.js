'use strict';

describe('Directive: zfSearchMap', function () {
  beforeEach(module('ZeitfadenApp'));

  var element;

  it('is not to expect anything, google maps texting, how?', inject(function ($rootScope, $compile) {
    //element = angular.element('<zf-search-map></zf-search-map>');
    //element = $compile(element)($rootScope);
    //expect(element.text()).toBe('this is the zfSearchMap directive');
  }));
});
