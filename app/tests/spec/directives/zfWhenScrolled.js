'use strict';

describe('Directive: zfWhenScrolled', function () {
  beforeEach(module('ZeitfadenApp'));

  var element;

  it('has to be implemented', inject(function ($rootScope, $compile) {
    //element = angular.element('<zf-when-scrolled></zf-when-scrolled>');
    //element = $compile(element)($rootScope);
    //expect(element.text()).toBe('this is the zfWhenScrolled directive');
  }));
});
